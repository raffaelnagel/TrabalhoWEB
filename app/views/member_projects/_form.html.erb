<%= form_for(@member_project) do |f| %>
  <% if @member_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member_project.errors.count, "error") %> prohibited this member_project from being saved:</h2>

      <ul>
      <% @member_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :members_id %><br>
	<%= f.select :members_id, Member.all.collect{|u| [u.name, u.id]}  %>
  </div>
  <div class="field">
    <%= f.label :projects_id %><br>
    <%= f.select :projects_id, Project.all.collect{|u| [u.name, u.id]}  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
